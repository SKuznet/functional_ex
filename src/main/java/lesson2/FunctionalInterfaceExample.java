package lesson2;

// annotation not mandatory but recommended to control yourself
@FunctionalInterface
public interface FunctionalInterfaceExample {
    // abstract method should be only one
    abstract public void abstractmethod();
}
